# Agent Notes

> This proof is intentionally review-first: use the designer packet and context slices before editing source code.

Status: current

Audience: coding agents and maintainers.

Use when: starting work in this proof repo.

Rules:

- Run `npm run verify` before committing a checkpoint.
- After `topo/` appears, update Topogram records first, then the maintained React app.
- Use `proof/artifacts/**` for generated proof packets.
- Do not commit local absolute paths, build output, or `node_modules/`.

